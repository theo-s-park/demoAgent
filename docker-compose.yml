services:
  tool-server:
    build: ./tool-server
    ports:
      - "8081:8081"
    env_file:
      - .env

  agent-server:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - TOOL_SERVER_URL=http://tool-server:8081
    depends_on:
      - tool-server
